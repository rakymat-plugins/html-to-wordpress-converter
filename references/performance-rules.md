# Performance Rules

Use Vite production builds. Verify:

```text
public/build/manifest.json
```

Avoid loading unused libraries, but do not remove a dependency if it changes required visual behavior. Optimize images after visual parity is proven.

