# Editor Experience Rules

ACF fields should be safe and clear for editors:

- meaningful labels
- instructions where the field affects layout
- repeaters with min/max when design depends on item count
- image fields with preview sizes
- link fields for CTAs
- sensible default values where needed

Editor flexibility must not allow accidental visual breakage unless the user explicitly wants flexible layouts.

